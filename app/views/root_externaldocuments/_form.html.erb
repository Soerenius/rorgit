<%= form_with(model: root_externaldocument, local: true) do |form| %>
  <% if root_externaldocument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(root_externaldocument.errors.count, "error") %> prohibited this root_externaldocument from being saved:</h2>

      <ul>
        <% root_externaldocument.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
