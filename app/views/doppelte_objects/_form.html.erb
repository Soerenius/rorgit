<%= form_with(model: doppelte_object, local: true) do |form| %>
  <% if doppelte_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doppelte_object.errors.count, "error") %> prohibited this doppelte_object from being saved:</h2>

      <ul>
        <% doppelte_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
