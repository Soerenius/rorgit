<%= form_with(model: root_object, local: true) do |form| %>
  <% if root_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(root_object.errors.count, "error") %> prohibited this root_object from being saved:</h2>

      <ul>
        <% root_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
