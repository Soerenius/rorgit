<%= form_with(model: relassignmeasure, local: true) do |form| %>
  <% if relassignmeasure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relassignmeasure.errors.count, "error") %> prohibited this relassignmeasure from being saved:</h2>

      <ul>
        <% relassignmeasure.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
