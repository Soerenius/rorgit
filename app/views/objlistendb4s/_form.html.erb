<%= form_with(model: objlistendb4, local: true) do |form| %>
  <% if objlistendb4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(objlistendb4.errors.count, "error") %> prohibited this objlistendb4 from being saved:</h2>

      <ul>
        <% objlistendb4.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
