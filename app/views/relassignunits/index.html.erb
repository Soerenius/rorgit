<p id="notice"><%= notice %></p>

<h1>Relassignunits</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @relassignunits.each do |relassignunit| %>
      <tr>
        <td><%= link_to 'Show', relassignunit %></td>
        <td><%= link_to 'Edit', edit_relassignunit_path(relassignunit) %></td>
        <td><%= link_to 'Destroy', relassignunit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Relassignunit', new_relassignunit_path %>
