<%= form_with(model: relassignunit, local: true) do |form| %>
  <% if relassignunit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relassignunit.errors.count, "error") %> prohibited this relassignunit from being saved:</h2>

      <ul>
        <% relassignunit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
