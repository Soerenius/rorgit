<%= form_with(model: object_table, local: true) do |form| %>
  <% if object_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(object_table.errors.count, "error") %> prohibited this object_table from being saved:</h2>

      <ul>
        <% object_table.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
