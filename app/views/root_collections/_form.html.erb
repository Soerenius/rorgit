<%= form_with(model: root_collection, local: true) do |form| %>
  <% if root_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(root_collection.errors.count, "error") %> prohibited this root_collection from being saved:</h2>

      <ul>
        <% root_collection.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
